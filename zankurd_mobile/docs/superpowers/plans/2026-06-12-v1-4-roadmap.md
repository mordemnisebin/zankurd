# ZanKurd v1.4 Geliştirme Planı

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Onaylanan tüm iyileştirmeleri (hijyen, içerik, bağlılık özellikleri, UX modernleştirme) v1.4 kapsamında eksiksiz uygulamak.

**Architecture:** Mevcut repository pattern korunur; tüm yeni kalıcı durum (görülen sorular, streak, yanlışlar, rozetler, onboarding, tema) `shared_preferences` üzerinde yerel store sınıflarıyla tutulur — Supabase şeması DEĞİŞMEZ (kullanıcı prod SQL'i kendisi uygular kuralı gereği sunucu işi bu sürümde yok). Tema ve dil Provider ile yönetilir.

**Tech Stack:** Flutter 3.x, provider, shared_preferences, flutter_localizations + ARB (son faz).

**Kalite çıtası:** Her faz sonunda `flutter analyze` 0 uyarı + `flutter test` yeşil (C:\src\zankurd_mobile kopyasında, TMP/TEMP=C:\src\tmp). Commit'ler Desktop deposunda, mantıksal ve küçük. Push yok.

---

## Faz 0 — Hijyen (tamamlandı: bekleyen işler 4 commit'e bölündü)

- [x] Bekleyen değişiklikler commit'lendi (cc45908, 2cf29c3, d642e82, 7a1cefa)

## Faz 1 — Gözlemlenebilirlik + dosya bölme

### Task 1: Crashlytics sessiz catch kayıtları
**Files:** Create `lib/src/utils/error_reporter.dart`; Modify `home_screen.dart`, `profile_screen.dart`, `favorite_questions_screen.dart` (sessiz `catch (_)` noktaları)
- `ErrorReporter.record(error, stack, reason)` — kIsWeb/test ortamında no-op, aksi halde `FirebaseCrashlytics.instance.recordError`.
- Davranış değişmez: catch'ler yine yutulur ama artık kaydedilir.

### Task 2: home_screen bölme
**Files:** Create `lib/src/screens/home/` altında `home_header.dart`, `hero_card.dart`, `daily_quiz_card.dart`, `spin_wheel_card.dart`, `room_actions.dart`, `category_grid.dart`, `question_card.dart`, `stats_row.dart`; Modify `home_screen.dart` (yalnız state + kompozisyon kalır)
- Saf taşıma; görsel/davranış değişikliği yok. Mevcut testler aynen geçmeli.

## Faz 2 — İçerik: tekrar önleme + soru havuzu

### Task 3: SeenQuestionStore + tekrar önleyen seçim
**Files:** Create `lib/src/data/seen_question_store.dart`; Modify `mock_zankurd_repository.dart`, `supabase_zankurd_repository.dart` (seçim sonrası filtre/önceliklendirme), `quiz_screen.dart` (soru gösterildiğinde işaretle)
- Store: `Set<String>` soru id'leri, kategori bazlı sayaç, `markSeen(ids)`, `filterPreferUnseen(pool, limit)` — görülmemişler önce; havuz yetmezse en eski görülenlerle tamamla; tüm havuz görüldüyse sıfırla.
- Birim testleri: önceliklendirme, sıfırlama, kalıcılık.

### Task 4: Soru bankası genişletme
**Files:** Modify `lib/src/data/offline_question_bank.dart`
- Mevcut 987 soru şablon üretimli (kelime başına 4-5 varyant). Her kategoriye şablon-dışı ÖZGÜN sorular eklenir (hedef: +300, zorluk 1-5 dengeli; Çand/Dîrok/Edebiyat/Cografya/Muzîk bilgi soruları + Ziman dilbilgisi soruları).
- ID şeması: `offline_2xxx` (çakışma yok). Doğruluk önceliklidir; emin olunmayan içerik eklenmez.
- Test: id benzersizliği, correctAnswer ∈ answers, kategori geçerliliği (bank bütünlük testi).

## Faz 3 — Bağlılık özellikleri

### Task 5: Günlük streak
**Files:** Create `lib/src/data/streak_store.dart`; Modify `quiz_result_screen.dart` (gün tamamlama tetikleyici), `home/home_header.dart` (🔥 göstergesi)
- Kurallar: aynı gün ikinci oyun streak'i artırmaz; dün oynanmamışsa 1'e döner. UTC değil yerel gün.

### Task 6: Yanlışlardan öğrenme modu
**Files:** Create `lib/src/data/mistake_store.dart`; Modify `quiz_screen.dart` (yanlışta kaydet, çalışma modunda doğru cevapta düşür), `profile_screen.dart` (giriş noktası + sayaç)
- Çalışma modu: mevcut `QuizScreen`'e `practiceMistakes` bayrağı yerine yanlış id'lerinden kurulan soru listesiyle normal akış; coin ödülü YOK (suistimal önleme).

### Task 7: Bot rakipli yarış
**Files:** Create `lib/src/game/bot_opponent.dart`; Modify `quiz_screen.dart` (yarış modunda bot skor simülasyonu), `quiz_result_screen.dart` (karşılaştırma paneli)
- Bot: soru başına doğruluk olasılığı zorlukla ölçekli (ör. p=0.85-difficulty*0.08), cevap süresi rastgele; deterministik test için seed enjekte edilebilir.

### Task 8: Rozetler
**Files:** Create `lib/src/data/achievement_store.dart`, `lib/src/models/achievement.dart`; Modify `quiz_result_screen.dart` (kazanım kontrolü + bildirim), `profile_screen.dart` (vitrin)
- İlk set (yerel): İlk Oyun, 10 Doğru Üst Üste, 100 Soru, Her Kategoride Oyun, 7 Gün Streak, Yanlışlarını Temizledi, Bot'u Yendi, Günlük Quiz x5.

## Faz 4 — UX modernleştirme

### Task 9: Onboarding
**Files:** Create `lib/src/screens/onboarding_screen.dart`; Modify `main.dart`/`app_shell.dart` (ilk açılış kontrolü, shared_preferences bayrağı)
- 3 sayfa: Hoş geldin/öğren, Yarış & liderlik, Günlük ödüller. Atla + Başla.

### Task 10: Açık tema + tema seçici
**Files:** Modify `app_theme.dart` (palet soyutlaması: `AppPalette` dark/light + statik getter köprüsü), Create `lib/src/providers/theme_provider.dart`; Modify `main.dart`, `settings_screen.dart` (Koyu/Açık/Sistem seçici); tüm `const` renk kullanımlarının derleyici eşliğinde düzeltilmesi
- En riskli refactor: `AppTheme.bg` vb. statikleri getter'a çevirince const ifadeler kırılır → mekanik düzeltme. Görsel regresyon için smoke test koşulur.

### Task 11: ARB i18n migrasyonu
**Files:** Create `l10n.yaml`, `lib/l10n/app_ku.arb`, `lib/l10n/app_tr.arb`; tüm ekranlarda `context.s(ku, tr)` → `AppLocalizations` geçişi; `lang.dart` locale yönetimine dönüşür
- En son yapılır (önceki fazların string'leri de tek seferde migre edilir). `ku` locale kodu `Locale('ku')` ile desteklenir.

## Faz 5 — Final doğrulama

### Task 12: Sürüm + doğrulama
- `pubspec.yaml` → 1.4.0+5, `release_readiness.md` + sürüm notları güncelle.
- Tam koşu: analyze 0, tüm testler yeşil, Android release build duman testi (kullanıcı isterse).
