# Android Keystore Configuration
# Copy this file to key.properties (in the same android/ folder) and fill in your values.
# IMPORTANT: Never commit key.properties to git — it's in .gitignore.

storePassword=YOUR_KEYSTORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=zankurd
storeFile=../zankurd-release.jks
