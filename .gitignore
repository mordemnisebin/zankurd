# Büyük medya ve çıktı klasörleri
görseller/
Pirs_apk_extracted/
output/
*.zip

# Başka bir ajan/araç tarafından proje içine yazılan oturum log'ları
# (üretim kodu değil; repo dışına taşınması gerekir ama şimdilik yok sayılıyor)
zankurd_mobile/bash_events/
zankurd_mobile/conversations/

# Kök seviyedeki oturum ekran görüntüleri (ürün asset'i değil)
/tour-*.jpeg
/live-*.jpeg
/fazA-*.jpeg
/fazB-*.jpeg
/pirs_*.jpg
/zankurd.png

# Mobil ve test araçlarının bağımlılıkları/önbellekleri
.playwright-mcp/
.agents/
.claude/
.codex/
.vscode/

# Eğer varsa sanal ortam klasörleri
venv/
# Ortam değişkenleri ve gizli dosyalar
.env
.env.*
!.env.example
# .env.deploy (FTP şifresi içerir) — deploy-to-hostinger.sh yerelde okur.
.env.deploy
# Oturum artıkları ve yerel araç dizinleri
.serena/
.playwright-cli/
pirs_dump.xml
/pirs_*.png

# Repo dışı kalması gereken tasarım inceleme ve audit araçları
audit/
design_review/
phase1-desktop-current.png
**/test/golden/failures/
/.worktrees/

.aider*

# macOS klasor metadata cop dosyalari
.DS_Store
**/.DS_Store

# Python derleme cache
__pycache__/
*.pyc

# Node bağımlılıkları ve araç çıktıları.
# `sky_bird/` 74 MB'lık bir `node_modules` taşıyor ve `git status` her
# çağrıda onunla doluyordu; kaynağın kendisi bilerek görünür bırakıldı —
# ayrı bir repoya mı taşınacak yoksa buraya mı commit edilecek, bu karar
# ürün sahibinin (2026-08-16).
node_modules/
**/node_modules/
**/test-results/
**/playwright-report/

# Supabase CLI'nin yerel çalışma artığı. Ortama özgü bağlantı bilgisi
# tutar; kökte de mobil klasörde de üretilir. `.branches` da aynı sınıfta:
# yerel stack'in o anki dal adını tutar, paylaşılacak bir şey değildir.
supabase/.temp/
**/supabase/.temp/
supabase/.branches/
**/supabase/.branches/

# Denetim kanıtları: ekran görüntüleri, build/test logları, ham veri dökümleri.
# Diskte kalır (raporlar buraya yol verir) ama git'e GİRMEZ — hem 36 MB'lık
# ikili yığın, hem de logların içinde ortama özgü yollar ve kimlikler var.
# Yedeği: ~/Documents/ZanKurd-Backups/zankurd-audit-*.tar.gz
audit_artifacts/
